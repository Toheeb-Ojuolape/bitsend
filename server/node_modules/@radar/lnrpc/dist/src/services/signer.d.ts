import { ConnectionConfig } from '../types';
/**
 * Create a Signer GRPC service client proxy
 * @param config The grpc service configuration
 */
export declare function createSigner(config: ConnectionConfig): any;
