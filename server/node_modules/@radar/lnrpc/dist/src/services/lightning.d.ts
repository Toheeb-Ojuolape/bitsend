import { ConnectionConfig } from '../types';
/**
 * Create a Lightning GRPC service client proxy
 * @param config The grpc service configuration
 */
export declare function createLightning(config: ConnectionConfig): any;
