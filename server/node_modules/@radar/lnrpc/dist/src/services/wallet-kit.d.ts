import { ConnectionConfig } from '../types';
/**
 * Create a WalletKit GRPC service client proxy
 * @param config The grpc service configuration
 */
export declare function createWalletKit(config: ConnectionConfig): any;
