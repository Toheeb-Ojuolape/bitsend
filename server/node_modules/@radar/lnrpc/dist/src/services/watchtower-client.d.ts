import { ConnectionConfig } from '../types';
/**
 * Create a WatchtowerClient service client proxy
 * @param config The grpc service configuration
 */
export declare function createWatchtowerClient(config: ConnectionConfig): any;
