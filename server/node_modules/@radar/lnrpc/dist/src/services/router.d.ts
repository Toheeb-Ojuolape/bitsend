import { ConnectionConfig } from '../types';
/**
 * Create a Router GRPC service client proxy
 * @param config The grpc service configuration
 */
export declare function createRouter(config: ConnectionConfig): any;
