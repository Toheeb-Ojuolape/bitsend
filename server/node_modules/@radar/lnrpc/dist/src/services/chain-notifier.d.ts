import { ConnectionConfig } from '../types';
/**
 * Create a Chain Notifier GRPC service client proxy
 * @param config The grpc service configuration
 */
export declare function createChainNotifier(config: ConnectionConfig): any;
