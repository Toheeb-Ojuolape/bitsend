import { ConnectionConfig } from '../types';
/**
 * Create a Invoices GRPC service client proxy
 * @param config The grpc service configuration
 */
export declare function createInvoices(config: ConnectionConfig): any;
