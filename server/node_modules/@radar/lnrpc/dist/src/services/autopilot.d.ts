import { ConnectionConfig } from '../types';
/**
 * Create a Autopilot GRPC service client proxy
 * @param config The grpc service configuration
 */
export declare function createAutopilot(config: ConnectionConfig): any;
