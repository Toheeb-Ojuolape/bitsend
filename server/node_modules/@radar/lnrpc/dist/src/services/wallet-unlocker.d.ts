import { ConnectionConfig } from '../types';
/**
 * Create a wallet unlocker GRPC service client proxy
 * @param config The grpc service configuration
 */
export declare function createWalletUnlocker(config: ConnectionConfig): any;
