import { ConnectionConfig } from '../types';
/**
 * Create a Watchtower GRPC service client proxy
 * @param config The grpc service configuration
 */
export declare function createWatchtower(config: ConnectionConfig): any;
